name: Set Project Month

on:
  pull_request:
    types: [opened, ready_for_review, reopened]

jobs:
  set-month:
    uses: datum-cloud/actions/.github/workflows/set-project-month.yaml@2dbca23d057e20ff1aab144b94a06d09ea75e5f6 # v1.21.0
    secrets: inherit
