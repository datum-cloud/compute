# provider

Reserved for compute-authored UI building blocks meant to be consumed by
other services' portal plugins or by cloud-portal itself — as opposed to
[`ui/consumer`](../consumer), which holds compute's own portal plugin(s).

- [`workloads`](./workloads) — a platform-wide, staff-facing Workloads
  plugin loaded by `staff-portal` (not cloud-portal).
