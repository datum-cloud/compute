---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: instancetypes.compute.datumapis.com
spec:
  group: compute.datumapis.com
  names:
    kind: InstanceType
    listKind: InstanceTypeList
    plural: instancetypes
    singular: instancetype
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.lifecycle.phase
      name: Phase
      type: string
    - jsonPath: .spec.resources.cpu
      name: CPU
      type: string
    - jsonPath: .spec.resources.memory
      name: Memory
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha
    schema:
      openAPIV3Schema:
        description: InstanceType is the Schema for the instancetypes API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InstanceTypeSpec defines the desired state of InstanceType
            properties:
              description:
                description: Description is a human-friendly description of the instance
                  type.
                type: string
              displayName:
                description: DisplayName is a human-friendly display name.
                type: string
              lifecycle:
                description: Lifecycle declares the operator-managed lifecycle of
                  this tier.
                properties:
                  phase:
                    description: |-
                      Phase defines the lifecycle phase of this instance type.
                      Valid values are "Active", "Deprecated", and "Disabled".
                      The lifecycle progression must follow Active -> Deprecated -> Disabled;
                      transitions cannot skip states or go backwards.
                    enum:
                    - Active
                    - Deprecated
                    - Disabled
                    type: string
                  replacementInstanceType:
                    description: ReplacementInstanceType specifies the optional successor
                      tier when Phase is Deprecated or Disabled.
                    type: string
                required:
                - phase
                type: object
              resources:
                description: Resources defines the core dimensions of the instance
                  type.
                properties:
                  cpu:
                    anyOf:
                    - type: integer
                    - type: string
                    description: CPU specifies the compute capacity (e.g. "2" or "2000m")
                      representing corev1.ResourceCPU.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  memory:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Memory specifies the memory capacity (e.g. "4Gi"
                      or "4096Mi") representing corev1.ResourceMemory.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - cpu
                - memory
                type: object
            required:
            - lifecycle
            - resources
            type: object
          status:
            description: InstanceTypeStatus defines the observed state of InstanceType
            properties:
              conditions:
                description: Conditions hold the latest available observations of
                  the InstanceType's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deprecatedAt:
                description: |-
                  DeprecatedAt records when this instance type entered the Deprecated phase.
                  It is set by the operator and used to gate the Deprecated -> Disabled
                  transition on the configured grace period.
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
